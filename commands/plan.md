# plan

Purpose:
- define work split before assigning workers

When to use:
- large or multi-file tasks
- multi-agent routing

Inputs:
- user request
- current registry/report

Outputs:
- scoped task contracts

Safety rules:
- do not dispatch vague tasks

Example:
- build a task contract before engineering and review handoff

